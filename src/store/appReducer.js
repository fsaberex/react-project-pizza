export const appReducer = (state, action) => {

    const {type, payload} = action;

    switch (type) {
        case 'searchIngredient':
            return state

        case 'searchRecipe':
            return state
    }

}